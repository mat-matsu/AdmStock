@model IEnumerable<AdmStock.Models.Articulo>

@{
    ViewData["Title"] = "Index";
}

<h1>@ViewBag.Message</h1>

<p>
    <a asp-action="Create">
        <input type="button" value="Alta nuevo Articulo" class="btn btn-info" />
    </a>
</p>

@using (Html.BeginForm())
{
    <ul id="searchForm">
        <li>
            <label class="form-label">Buscar por Tipo de Producto: </label>
            @Html.TextBox("searchStrArt", null, new {@class="form-control"})
        </li>
        <li><input type="submit" value="Search" class="btn btn-info" /> </li>
    </ul>
}

<table class="table table-dark table-striped table-hover">
    <thead>
        <tr>
            <th scope="col">
                @Html.ActionLink("Tipo de Producto", "Index", new { sortOrder = @ViewBag.ArtSortParm}, null)
            </th>
            <th scope="col">Opciones</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tipo_prod)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.art_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.art_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.art_id">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>
